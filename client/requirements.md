## Packages
framer-motion | Smooth animations for page transitions and hover effects
react-icons | Specific gaming icons if Lucide is insufficient (optional, Lucide usually covers it)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
